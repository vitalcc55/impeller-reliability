import { createTheme } from '@mantine/core';

export const impellerTheme = createTheme({
  fontFamily: '"Segoe UI", Arial, sans-serif',
  headings: { fontFamily: '"Segoe UI", Arial, sans-serif' },
  cursorType: 'pointer',
  respectReducedMotion: true,
});
